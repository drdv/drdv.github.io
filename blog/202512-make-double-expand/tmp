# n0 = lambda f: lambda x: x
# n1 = lambda f: lambda x: f(x)
# n2 = lambda f: lambda x: f(f(x))
# inc = lambda x:x+1
# succ = lambda n: lambda f: lambda x: f(n(f)(x))

# n2 = λf. λx. f(f(x))
n2 = $(call n2-inner,$(1),$(2))
n2-inner = $(call $(1),$(call $(1),$(2)))

# n1 = $(call or,$f,$(call or,$f))

f = $$x+1
x = |

result = $(call n2,f,x)
$(info $(result))
